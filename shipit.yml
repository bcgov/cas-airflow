dependencies:
  override: []
deploy:
  override:
    - ./helm/cas-airflow/patch-values.sh
    - helm dep up ./helm/postgresql
    - helm dep up ./helm/airflow
    - helm dep up ./helm/cas-airflow
    - helm upgrade --install --atomic --timeout 600s --namespace "$ENVIRONMENT" cas-airflow ./helm/cas-airflow
review:
  checklist: []
